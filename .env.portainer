# Variáveis de Ambiente para Portainer Stack
# ==========================================

## OBRIGATÓRIAS



# Domínio onde a API será acessada (Usado para o certificado HTTPS automático)
DOMAIN=api.seudominio.com.br

# URL do seu projeto Supabase (Configurações -> API -> URL)
SUPABASE_URL=https://your-project.supabase.co

# Chave "service_role" do Supabase (Configurações -> API -> Service Role Key)
# CUIDADO: Esta chave tem permissão total, não compartilhe!
SUPABASE_KEY=your_service_role_key_here

# Chave da API do Google Gemini (aistudio.google.com)
GEMINI_API_KEY=your_gemini_api_key_here

# Senha para proteger sua API (Header X-API-Key). 
# Escolha uma senha forte. Se deixar vazio, a API fica pública!
API_KEY=your_secret_password

## OPCIONAIS (Configurações Avançadas)

# Quantos produtos retornar na busca por padrão
PRODUCTS_LIMIT=5

# Limite de requisições simultâneas para o Gemini (Evita erro 429 Too Many Requests)
MAX_CONCURRENT_AI_REQUESTS=5

# Tempo máximo (segundos) que uma requisição espera na fila da IA
AI_QUEUE_TIMEOUT=30

# Intervalo (minutos) que o Worker verifica novos produtos para criar embeddings
EMBEDDING_UPDATE_INTERVAL_MINUTES=10
